SHOPIFY_STORE_DOMAIN=hyzdg1-5f.myshopify.com
SHOPIFY_API_VERSION=2024-10

# 2026 Dev Dashboard apps: use Client ID + Client Secret (auto-exchanged for a 24h token).
SHOPIFY_CLIENT_ID=
SHOPIFY_CLIENT_SECRET=

# Optional legacy static token. If set, it overrides the client credentials grant.
SHOPIFY_ADMIN_API_TOKEN=

CMS_PASSWORD=
JWT_SECRET=

# CMS-owned data store (ETA, material grams, dimensions, production stages).
# Provision via Vercel Storage -> Marketplace Database Integrations -> Upstash Redis
# (or an Upstash account directly). Values are auto-injected once connected to the project.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
